import { Firestore } from "firebase-admin/firestore";

// eslint-disable-next-line require-jsdoc
export async function endTurn(db: Firestore, gameId: string) {
  return new Promise((res) => res("Not yet implemented"));
}
